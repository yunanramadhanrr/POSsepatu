[build]
builder = "NIXPACKS"

[build.nixpacks]
command = "echo 'PHP installed successfully'"

[deploy]
startCommand = "php -S 0.0.0.0:$PORT -t public"

[deploy.environmentVariables]
DOCUMENT_ROOT = "public"
PHP_EXTENSIONS = "mysqli,pdo,pdo_mysql,gd,zip,curl,mbstring"